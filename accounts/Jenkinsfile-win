node('inovate') {
    checkout scm    
        stage("Build") {
            sh 'cd accounts/'
            sh 'npm install'
        }

        stage("Deploy") {
        
            sh 'npm start'
            
        }
        
    }

